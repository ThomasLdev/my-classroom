twig_component:
    anonymous_template_directory: 'components/'
    defaults:
        # Namespace & directory for components
        App\Twig\Components\: 'components/'
        # Composants PHP-backed dans leur contexte hexagonal (UI = adaptateur pilotant)
        App\Teaching\Infrastructure\Http\Component\: 'components/'
