apiVersion: v1
kind: Service
metadata:
  name: my-classroom
  namespace: my-classroom
spec:
  type: NodePort
  selector:
    app: my-classroom
  ports:
    - name: http
      port: 80
      targetPort: 80
      # 30080=tinie-bakerie, 30081=poke-game; my-classroom uses 30082.
      nodePort: 30082
